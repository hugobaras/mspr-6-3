version: "3"

services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: wildlens
      MYSQL_USER: wildlens_user
      MYSQL_PASSWORD: iEI8NTMn7vDzTng
      MYSQL_INIT_FILE: /docker-entrypoint-initdb.d/wildlens-container.sql
    ports:
      - "3306:3306"
    volumes:
      - /Users/hugobaras/dumps:/docker-entrypoint-initdb.d

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend_test
    ports:
      - "3001:3001"
    depends_on:
      - mysql
  frontend:
    image: mspr_6_3_front_end:2.0
    ports:
      - "80:80"
    depends_on:
      - backend
